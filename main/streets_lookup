<?php
//main/streets_lookup.php
// Streets Lookup API Endpoint
// Returns JSON list of streets matching a query
// Expects a 'q' query parameter with the search term
// format : street name, suburb, localbound, province, country
// one line search like google maps, where your dividers are commas and you can search for any part of the address
// eg. 14, pretorius street, discovery, city of johannesburg, gauteng, south africa

// or reverse lookup with dropdowns and cascading selects
// select country - shows list of countries
// select province - shows list of provinces in that country    
// select localbound - shows list of localbounds in that province
// select suburb - shows list of suburbs in that localbound 
// select street - shows list of streets in that suburb
// select street number  - free text input.

// ajax: include/ajax/get_streets_data.php 
// js: include/js/streets_lookup.js
// use secure/dbconfig.php  - set db credentials and connection string 
// use secure/db_connect.php Standard PDO connection for production mode, Requires db_config.php to define:



// use open street map api to get the data or any other free geocoding api that allows for reverse geocoding and has a large database of streets in south africa


declare(strict_types=1);

//Table structure:
//use louisvr_portfolio_dev;

//create table str_address (
//id int NOT NULL AUTO_INCREMENT primary key,
//str_number varchar(10),
//str_name varchar(200),
//suburb varchar(200),
//city varchar(200),
//province varchar(200),
//country varchar(200)
//);

//select * from louisvr_portfolio_dev.str_address;


